<%= form_with(model: objeto) do |form| %>
  <% if objeto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(objeto.errors.count, "error") %> prohibited this objeto from being saved:</h2>

      <ul>
        <% objeto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class = "container">
      <div class="form-group">
    <%= form.label :tipo, style: "display: block" %>
    <%= form.select(:tipo, ['Microcontrolador','Drone', 'Placa', 'Chip','Peças'], {}, { :class => 'form-control' }) %>
  <div class="form-group">
    <%= form.label :descricacao, style: "display: block" %>
    <%= form.text_area :descricacao, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :condicao, style: "display: block" %>
    <%= form.text_field :condicao, class: "form-control" %>
  </div>

  <br>

  <div class="form-group">
    <%= form.submit :Enviar, class: 'btn btn-success'%>
  </div>
  </div>

<% end %>
